# Verification — Connected single-phone journey — 2026-09-21

ผลตรวจ static build ใน Google Chrome ผ่าน Playwright ที่ project path `/kplus-scambreak-demo/` ไม่ใช่แค่ตรวจว่า build ผ่าน

## ผลการตรวจ

| สิ่งที่ตรวจ | ผล |
| --- | --- |
| เส้นทางหลัก | หน้าแรก → ผู้รับ → ใส่ยอด → ความเสี่ยง → พักรายการ → โหมดฉุกเฉิน → เลือกรายการ → แจ้งเหตุ → ติดตาม → หลักฐาน → กลับหน้าแรก |
| ทั้งสองฟีเจอร์ | อยู่ใน in-memory state เดียว ไม่ต้องเปลี่ยนฉากหรือรีเซ็ต |
| หน้าปลายทาง | ทดสอบครบ 36 routes และกดปุ่มมากกว่า 110 รูปแบบ ครอบคลุมทุกกลุ่มปุ่มหน้าแรก |
| กรอบเครื่อง | มีเพียงเครื่องเดียว ตำแหน่งและสัดส่วนคงเดิมระหว่างเปลี่ยนหน้า |
| Screenshot หน้าต้นฉบับทั้ง 6 | ที่ 388 × 778 เปรียบเทียบภาพต้นฉบับใน browser ขนาดเดียวกัน: ความต่าง 0 พิกเซลในการทดสอบรอบที่บันทึก |
| หน้าที่เพิ่ม | ตรวจภาพหน้าใส่ยอด รายการก่อนหน้า แจ้งเหตุ หลักฐาน แชต โทร ความปลอดภัย และบริการย่อย |
| กรอกยอด | แป้นตัวเลข/พิมพ์/ยอดด่วน/ลบ/ล้างใช้งานได้ ตรวจยอดว่าง ทศนิยม และยอดเกินคงเหลือ |
| หน้าตรวจสอบ | แสดงยอดที่กรอกจริง ตัวอย่าง 12,345.67 บาท โดยแทนเฉพาะช่องตัวเลข |
| คำถาม | เลือกใช่/ไม่ใช่และย้อนกลับได้ ต้องตอบครบก่อนดูคำแนะนำ |
| พักรายการ | Countdown ถึง 0 แล้วเงินยังไม่ออก ยอดคงเหลือไม่เปลี่ยน ไม่มีการโอนต่ออัตโนมัติ |
| ยกเลิกรายการ | มีหน้ายืนยัน/กลับไปพัก/ผลยกเลิก เปลี่ยนสถานะของรายการที่กำลังทำจริง |
| การแจ้งเหตุ | ต้องเลือกรายการ เหตุการณ์ และความยินยอมในเดโมก่อนตรวจทานและยืนยัน |
| เงินสองรายการ | ยอดที่ผู้ใช้ใส่ยังพักไว้ แยกจากรายการก่อนหน้า 50,000 บาทที่นำไปแจ้งเหตุ |
| เคส | สร้างหลังยืนยันเท่านั้น ไม่สร้างซ้ำ กลับหน้าแรกแล้วเปิดเคสเดิมได้ |
| สถานะป้องกัน | คงอยู่ข้ามหน้าและการกดย้อนกลับ บล็อกโอน/สแกน/เติมเงิน/จ่ายบิล/ถอนเงิน |
| หลักฐาน | ดูตัวอย่างได้ เลือกเพิ่ม/เปลี่ยนได้ถึง 4 ประเภท จำนวนหลักฐานในหน้าติดตามอัปเดตจริง |
| ติดต่อ | แชต 3 หัวข้อและโทรจำลองมีสถานะเชื่อมต่อ/วางสาย ไม่มีการติดต่อจริง |
| ความปลอดภัย | ดู/จบเซสชันอุปกรณ์ตัวอย่างและทำเครื่องหมายในรายการแนะนำได้ ไม่รับรหัส |
| บริการหน้าแรก | ทุกไอคอน กระดิ่ง และแถบล่างมีปลายทาง เติมเงิน/จ่ายบิล/ถอนเงินจบด้วยใบรายการและประวัติจำลอง |
| Pause / Resume | หยุดเวลาและภาพจริงของ countdown, fade, ripple, การป้องกัน, รับแจ้งเหตุ และเชื่อมต่อจำลอง แล้วเล่นต่อจากจุดเดิม |
| Reset / Back / Next | ล้างสถานะทั้งหมดเมื่อ reset; ย้อน/เดินหน้ามี validation ไม่มี timer หรือหน้าจอเก่าค้าง |
| อัตโนมัติ | สาธิตครบ 24 ขั้นด้วย action เดียวกับปุ่มจริง ปิดเล่นต่อเนื่องเพื่อหยุดทีละขั้น หรือกดแทรกควบคุมเองได้ |
| Keyboard | Space, ←, →, R ผ่าน ไม่รบกวนการพิมพ์ยอด |
| Resize / Fullscreen | ย่อขยายทั้งเครื่องพร้อมกัน hotspot ตรงกับพิกัดปุ่ม ซ่อน/คืนแถบควบคุมได้ |
| Mobile 390 × 844, DPR 2 | แตะผ่านเส้นทางกรอกยอดถึงเคสและหลักฐานใน session เดียว ซูม 200% และเลื่อนได้ |
| Console / request / asset errors | 0 |
| ข้อมูลออกจากเดโม | เฉพาะ same-origin static GET ไม่มี API, POST, analytics หรือ external traffic |
| ช่องรับข้อมูลและ storage | ไม่มี file/password/tel input และไม่มี persistent storage |
| Unit tests | 38/38 รวม 22 tests ของเส้นทางใหม่และ 16 regression tests เดิม |
| Browser test groups | 11/11 |
| PPTX SHA-256 | เท่าเดิม: `aad8faa15e46fb619e3250e974b5200418f84959b16dcff5c53d44be96d22ebd` |

## วิธีทำซ้ำ

~~~sh
npm test
npm run build
npm run preview
python tests/browser_check.py http://127.0.0.1:4173/
~~~

ทดสอบ URL ที่เผยแพร่ด้วยชุดเดียวกันได้:

~~~sh
python tests/browser_check.py "https://blimmy.github.io/kplus-scambreak-demo/?v=journey-v2"
~~~

ภาพ screenshot, จำนวน pixel diff, routes, ปุ่มที่กด และ network log อยู่ใน `.work/journey-browser/report.json` และภาพข้างเคียง ไม่เผยแพร่ผลทดสอบใน repository

สถานะอ้างอิง: หน้าแรกก่อนเกิดเคส; หน้าตรวจสอบยอด 50,000 บาท; คำถามที่ตอบ “ใช่” ครบ; หน้าพักที่ `00:45`; โหมดฉุกเฉินที่สถานะครบ; หน้าติดตามที่มีหลักฐานเดิม 2 ชิ้น ทั้งหมดไม่มี hover, ripple หรือไฮไลต์ขณะเทียบ

ตัวตรวจภาพยอมให้ rounding จากการวาดของ GPU ได้ไม่เกิน 12 พิกเซลและแต่ละช่องสีต่างไม่เกิน 2/255 เพื่อไม่ให้การปัดเศษเพียงเล็กน้อยกลายเป็น false failure รายงานจะบันทึกจำนวนจริงทุกครั้ง

## ขอบเขตความเหมือน

ยืนยันภาพหน้าจอเดิมจาก PowerPoint เฉพาะสถานะและขนาดที่ตรวจ ไม่ได้อ้างว่าเหมือนองค์ประกอบทั้งสไลด์หรือ browser ทุกตัว รูปแบบมือถือเดียวเป็นความต่างที่ผู้ใช้ขอ ส่วนหน้าผู้รับ ใส่ยอด แบบฟอร์มแจ้งเหตุ และหน้าปลายทางปุ่มต่าง ๆ เป็นหน้าเพิ่มเพื่อเชื่อมเดโมให้ครบ โดยใช้ฟอนต์ สี และชิ้นภาพจากต้นฉบับ

ข้อมูลทั้งหมดสมมติ การหยุดเงินออกใช้กับบัญชีของผู้ใช้ เงินก่อนหน้ายังอยู่ระหว่างตรวจสอบ ไม่มีข้อความยืนยันการกู้เงินคืนหรืออายัดบัญชีปลายทางสำเร็จ
