# Verification — Single-phone revision — 2026-09-20

ตรวจด้วย Google Chrome ผ่าน Playwright โดยเปิด static build จริงจาก HTTP server ที่ project path `/kplus-scambreak-demo/` หลังปรับตามคำขอล่าสุดให้เป็นมือถือเครื่องเดียว

| สิ่งที่ตรวจ | ผล |
| --- | --- |
| พฤติกรรมเริ่มต้น | มีมือถือหนึ่งเครื่อง รอให้กด ไม่มี autoplay |
| ภาพหน้าจอทั้ง 6 ชิ้นที่ 776 × 1556 | เป็น crop จาก native PowerPoint export ตรงกันทุกพิกเซล |
| Screenshot สถานะอ้างอิงทั้ง 6 หน้าที่ 388 × 778 | เทียบภาพต้นฉบับที่แสดงใน browser ขนาดเดียวกัน ความต่างพิกเซล = 0 |
| กรอบเครื่องเมื่อเปลี่ยนหน้า | ตำแหน่งและขนาดเท่าเดิม มีกรอบเดียว หน้าจอเก่าถูกนำออกหลัง fade |
| SHA-256 ของ PPTX หลังสร้าง asset | เท่าเดิม ไม่มีการบันทึกกลับหรือแก้ต้นฉบับ |
| ฉาก 1 กดตรวจสอบ → ตอบ → คำแนะนำ → พักรายการ | ผ่าน; ต้องตอบครบก่อนดูคำแนะนำ |
| ตัวเลือก “ใช่” / “ไม่ใช่” | เปลี่ยนได้จริง และมี aria-pressed ที่ถูกต้อง |
| ลูกศรกลับในหน้าจอ | ย้อนหน้าและเก็บคำตอบไว้ |
| ฉาก 2 กดฉุกเฉิน → สถานะป้องกัน → แจ้งเหตุ → ติดตาม | ผ่าน; สถานะทยอยแสดงและปุ่มแจ้งเหตุรอให้สถานะครบ |
| Pause / Resume | หยุด/เดินต่อทั้ง countdown, fade, click ripple และการทยอยแสดงสถานะ ตรวจภาพก่อน/หลังหยุดแล้วเหมือนกัน |
| Countdown ถึงศูนย์ | อยู่หน้าพักรายการต่อ ไม่โอนเงินและไม่เปลี่ยนหน้าอัตโนมัติ |
| Reset | คืนหน้าแรกของฉาก ล้างคำตอบ เวลา transition และ frame เดิม ไม่เล่นต่อเอง |
| Next / Back / เปลี่ยนฉากระหว่างเล่น | ผ่าน; กลับมาควบคุมด้วยการกด ไม่มีนาฬิกาฉากเก่าค้าง |
| โหมดอัตโนมัติ | เล่นครบ 26 ขั้น (12 + 14) ในมือถือเครื่องเดียว แล้วหยุด |
| ปิดเล่นต่อเนื่อง | หยุดเมื่อจบแต่ละขั้น กดเล่นต่อแล้วไปขั้นถัดไป ไม่เริ่มหน้าเดิมซ้ำ |
| Keyboard | Space, ←, →, R ผ่าน |
| Desktop 1280 × 800 และ 1920 × 1080 | พิกัด hotspot ต่างจากค่าคำนวณไม่เกิน 0.1 CSS pixel ไม่มีการยืดผิดสัดส่วน |
| Fullscreen / ซ่อนแถบควบคุม | ผ่าน กดบนมือถือได้ และคืนแถบควบคุมได้ |
| Mobile 390 × 844, DPR 2 | เห็นทั้งเครื่อง แตะผ่านทั้งสองเส้นทางได้ ซูม 200% และเลื่อนดูรายละเอียดได้ |
| กรอบเครื่องบนมือถือระหว่างฉาก 1 | ขนาด 333.72 × 628 CSS pixels และตำแหน่ง (28, 8) เท่าเดิมทั้งสามหน้า |
| หลักฐาน / ติดต่อ | เปิดเฉพาะข้อความจำลอง ไม่มี file/password/tel input หรือการโทร |
| Console / request / HTTP asset errors | 0 |
| ข้อมูลออกจากเดโม | พบเฉพาะ same-origin static GET ไม่มี API, POST, analytics หรือบริการภายนอก |
| Node unit tests | ผ่าน 16/16 |
| Browser test groups | ผ่าน 11/11 |

ไฟล์ภาพและข้อมูล network อยู่ใน `.work/phone-browser/` (ไม่เผยแพร่ใน repository) เรียกซ้ำกับ URL ในเครื่องหรือ GitHub Pages ได้ด้วย:

~~~sh
python tests/browser_check.py URL
~~~

สถานะอ้างอิง: ฉาก 1 หน้าแรก / หน้าคำถามที่ตอบ “ใช่” ครบ / หน้าพักที่ `00:45`; ฉาก 2 หน้าแรก / โหมดฉุกเฉินที่แสดงสถานะครบ / หน้าติดตามเรื่อง ทั้งหมดไม่มี hover, ripple หรือไฮไลต์ขณะเทียบภาพ

ขอบเขตความเหมือน: ยืนยันเฉพาะภาพหน้าจอที่ crop จาก PowerPoint ด้วยฟอนต์ต้นฉบับและแสดงใน Chrome ที่ขนาดทดสอบ ไม่ใช่การอ้างว่าหน้าเว็บยังเหมือนองค์ประกอบทั้งสไลด์หรือ renderer ทุกตัวเหมือนกัน รูปแบบมือถือเดียว การเปลี่ยนหน้า คำตอบที่เลือกได้ และเลเยอร์เคลื่อนไหวเป็นความต่างที่ตั้งใจตามคำขอปรับล่าสุด
