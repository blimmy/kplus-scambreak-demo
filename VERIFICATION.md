# Verification — 2026-09-20

ตรวจด้วย Google Chrome ผ่าน Playwright โดยเปิด static build จริงจาก HTTP server ที่ project path `/kplus-scambreak-demo/`

| สิ่งที่ตรวจ | ผล |
| --- | --- |
| ภาพอ้างอิงฉาก 1 และฉาก 2 ที่ 1536 × 1024 | เทียบกับภาพต้นฉบับที่แสดงด้วย browser ที่ขนาดเดียวกัน ความต่างพิกเซล = 0 |
| ภาพหลักที่ใช้บนเว็บเทียบกับ native PowerPoint export อิสระ 3072 × 2048 | ความต่างพิกเซล = 0 ทั้งสองหน้า |
| SHA-256 ของ PPTX หลังทำ asset | เท่ากับก่อนเริ่ม ไม่มีการเปลี่ยนต้นฉบับ |
| การเล่นครบทั้งสองฉาก | ผ่านทั้งหมด 26 ขั้น (12 + 14) ตามลำดับ แล้วหยุด |
| กดตรวจสอบความเสี่ยง → คำตอบ 3 ข้อ → ดูคำแนะนำ | ผ่าน |
| กดฉันอาจถูกหลอก → โหมดฉุกเฉิน → แจ้งเหตุ → ติดตามเคส | ผ่าน |
| Pause | elapsed time, countdown และภาพอนิเมชั่นหยุดจริง ตรวจภาพก่อน/หลังรอแล้วเหมือนกัน |
| Resume | เดินต่อจาก elapsed time เดิม |
| Reset | ยกเลิก frame เดิม คืนขั้น -1 และ 00:45 ไม่เล่นต่อเอง |
| Next / Back / เปลี่ยนฉากระหว่างเล่น | ผ่าน ไม่มีเวลา/state จากฉากก่อนค้าง |
| ปิดเล่นต่อเนื่อง | เล่นจนจบเฉพาะขั้นปัจจุบันแล้วหยุด |
| Keyboard | Space, ←, →, R ผ่าน |
| Desktop 1280 × 800 และ 1920 × 1080 | พิกัด hotspot ต่างจากพิกัดที่คำนวณได้ไม่เกิน 0.1 CSS pixel |
| Fullscreen | เข้า/ออกและกด hotspot ได้ |
| ซ่อน/แสดงแถบควบคุม | ผ่าน แถบอยู่นอกพื้นที่สไลด์ |
| Mobile 390 × 844, DPR 2 | ทั้งสไลด์สัดส่วน 3:2, ซูม 300%, เลื่อนและกดได้ทั้งสองฉาก |
| ปุ่มหลักฐาน/ติดต่อ | เปิดข้อความจำลอง ไม่มี file input / password input หรือการโทรจริง |
| Console / request failures | 0 errors, 0 failed requests |
| ข้อมูลออกจากเดโม | พบเฉพาะ GET ของ static assets จาก origin เดียวกัน ไม่มี API, POST, analytics หรือบริการภายนอก |
| Automated timeline tests | ผ่าน 7/7 |

ไฟล์ภาพและข้อมูล network ที่ใช้ตรวจอยู่ใน `.work/browser/` ของโปรเจกต์ในเครื่อง (ไม่เผยแพร่ขึ้น repository) เรียกซ้ำได้ด้วย `python tests/browser_check.py URL`

ขอบเขตความเหมือน: ผล pixel comparison ยืนยันกับภาพสไลด์ที่ PowerPoint เรนเดอร์จาก PPTX ฉบับล่าสุดด้วยฟอนต์ชื่อเดิมครบทั้งไทย/Latin ไม่ใช่การอ้างว่าผลจาก renderer ทุกตัวจะเหมือนกันทุกพิกเซล ระหว่างเล่นมีเฉพาะเลเยอร์เน้น เคอร์เซอร์ การทยอยแสดงสถานะ และ countdown ที่ตั้งใจเพิ่มตามบรีฟ
